Requirement for run angular project(windows) :
1) install node js : https://nodejs.org/en/download/

2) go to powershell: 
check node version:
cmd: node --version
check npm version:
cmd: npm --version

3) install angular CLI:
cmd: npm install -g @angular/cli

4) for create angular project use :
cmd: ng new my-first-project
     cd my-first-project

5) for build app:
cmd:ng build my-app -c production

6) for run project use:
  cmd:ng serve

Requirement for run angular project(ubuntu) :
1) install node js
curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
sudo apt-get install -y nodejs

2)update npm
npm install -g npm@8.4.0

3)sudo npm install -g @angular/cli


4) for create angular project use :
cmd: ng new my-first-project
     cd my-first-project

5) for build app:
cmd:ng build my-app -c production

6) for run project use(go to test-project directory):
cmd:ng serve