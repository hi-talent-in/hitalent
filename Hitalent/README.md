1. turn on virtual environment if its linux ($ source env/bin/activate) else if its windows create virtual environment and activate.
2. now install dependencies with followin command
($ pip install -r requirements.txt)
3. run the django project server 
($ python3 manage.py runserver)